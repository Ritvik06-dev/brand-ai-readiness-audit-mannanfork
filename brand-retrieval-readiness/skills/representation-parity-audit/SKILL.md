---
name: representation-parity-audit
description: Analyze whether a page's important facts survive across representations (raw server HTML, visible text, inline state payloads, metadata) to detect content that a non-rendering AI crawler cannot retrieve. Normally invoked by audit-orchestrator; use alone only when asked specifically about rendering, hydration, or representation-loss concerns.
license: MIT
compatibility: Requires Python 3.9+ (standard library only).
metadata:
  version: "1.0.0"
---

# Representation Parity Audit

> **Build status (Phase 0):** stub. SKILL.md procedure and `scripts/analyze_representation.py`
> (snapshot-only per the dependency contract) land in Phases 2-3 per `BUILD_PLAN.md`.

## When to use

Stub - see description.

## Inputs

Stub: a snapshot path.

## Procedure

Stub.

## Output

Stub: finding fragments per the orchestrator's `finding_fragment.json`.
